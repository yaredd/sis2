<%= form_tag request.url , :method => :get do %>
  Period: <%= select_tag :period, options_for_select([[""], ["Sem2"], ["Sem1"]], params[:period]) %> Course: <%= select_tag :course_id, options_from_collection_for_select(Course.order(:name), :id, :name, params[:course_id]), :include_blank => true %>
	<%= submit_tag "apply" %>
<% end %>

